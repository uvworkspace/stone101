#include <iostream>
int main() {
    int x, y, sum;
    std::cout << "Please enter two integer values:";
    std::cin >> x >> y;
    sum = x + y;
    std::cout << x << " + " << y << " = " << sum << '\n';
}
